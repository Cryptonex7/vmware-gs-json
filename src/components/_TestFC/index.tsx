import "./styles.scss";

interface Props {
  title?: string;
  icon?: string;
  children?: React.ReactNode;
}

const TestFC: React.FC<Props> = (props: Props) => {
  return (
    <div>
      
    </div>
  );
};

export default TestFC;
